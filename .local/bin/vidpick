#!/bin/sh
# Pick a video and play with mpv

root_dir="$HOME/vids/lectures/"
dir=$(find $root_dir -type d | fzf --prompt="Directory: ") || exit
vid=$(find "$dir" -maxdepth 1 -type f \( -iname "*.mp4" -o -iname "*.webm" -o -iname "*.mkv" \) | fzf --prompt="Video: ") || exit
mpv "$vid"
